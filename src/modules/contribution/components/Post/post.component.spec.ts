import { Component, DebugElement, NO_ERRORS_SCHEMA } from '@angular/core';
import { ComponentFixture, TestBed } from '@angular/core/testing';

@Component({
    template: ` <sb-post> </sb-post> `,
})
class TestHostComponent {
    // someInput = 1;
    // someFunction(event: Event) {}
}
